// Examples of const
// The values of these variables will never change in future

const API_KEY = '344dsjroerorjkl'

const usersFullName = 'John Doe'

const bookDatabase = {
    bookName = 'The Secret',
    authorName = 'John Kennedy',
    bookPagesCount = 220,
    bookPrice = 500,
}



// Examples of let
// The values of these variables will change in future

let counterValue = 1
counterValue ++


let greenTeamPlayerScore = 100
if (greenTeamWon()) {
    greenTeamPlayerScore += 20
} else {
    greenTeamPlayerScore -= 5
}


let numberOfWebsiteVisitors = 100
if (usersFullName.visitedWebsite()) {
    numberOfWebsiteVisitors += 1
}

